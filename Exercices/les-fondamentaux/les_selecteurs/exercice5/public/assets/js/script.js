// Sélectionner la balise span
let selectSpan = document.querySelector('span');
console.log(selectSpan);

// Ajouter une classe à un DOM (page HTML)
selectSpan.classList.add("important");

// autre méthode
// selectSpan.setAttribute("style", "font-weight: bold");